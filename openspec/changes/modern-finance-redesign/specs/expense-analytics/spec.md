## MODIFIED Requirements

### Requirement: Category Spending Chart
The system SHALL display a visual chart showing total expenses grouped by category, using the application's defined pastel theme color palette.

#### Scenario: Displaying category breakdown
- **WHEN** the user opens the Statistics Screen
- **THEN** the system MUST display a chart where each category slice/bar uses its corresponding pastel color from the global theme.
