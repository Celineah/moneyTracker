## 1. Theme Infrastructure

- [x] 1.1 Create `constants/theme.ts` with pastel color palette, spacing scale, and shadow presets.
- [x] 1.2 Create a reusable `components/Card.tsx` component with rounded corners and soft shadows.

## 2. Component Refactoring

- [x] 2.1 Redesign `Header.tsx` to use the new typography and summary card style.
- [x] 2.2 Update `CategoryFilter.tsx` with pastel chips and better spacing.
- [x] 2.3 Redesign `ExpenseForm.tsx` into a clean card-based input section.
- [x] 2.4 Refactor `ExpenseList.tsx` and `ExpenseItem.tsx` to display expenses as cards instead of table rows.

## 3. Screen Layout Updates

- [x] 3.1 Update `HomeScreen.tsx` to implement the "Hero Card" summary at the top and the new card list.
- [x] 3.2 Update `StatisticsScreen.tsx` to use theme colors in the pie chart and category breakdown cards.
- [x] 3.3 Apply global background color and safe area adjustments for the new theme.

## 4. Final Polishing and Verification

- [x] 4.1 Fine-tune typography (weights and sizes) for an elegant "modern finance" feel.
- [x] 4.2 Verify visual consistency of shadows and rounded corners across all platforms (if applicable).
- [x] 4.3 Verify that all CRUD operations and analytics still function correctly with the new UI.
