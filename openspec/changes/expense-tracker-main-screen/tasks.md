## 1. Project Setup and Infrastructure

- [x] 1.1 Define TypeScript types for Expenses and Categories in a new `types/expense.ts` file.
- [x] 1.2 Prepare `App.tsx` by clearing boilerplate and setting up the basic container.

## 2. State and Logic Implementation

- [x] 2.1 Implement the `useExpenses` custom hook (or equivalent logic in `App.tsx`) to manage the expense list state.
- [x] 2.2 Add helper functions for adding, editing, and deleting expenses.
- [x] 2.3 Implement filtering logic based on the selected category.
- [x] 2.4 Implement calculation logic for Total Expenses and Remaining Balance.

## 3. UI Components Development

- [x] 3.1 Create a `Header` component for the title and summary stats.
- [x] 3.2 Create the `CategoryFilter` component (dropdown or chips).
- [x] 3.3 Create the `ExpenseForm` component for adding new entries.
- [x] 3.4 Create the `ExpenseList` and `ExpenseItem` components with the spreadsheet-inspired layout.

## 4. Integration and Refinement

- [x] 4.1 Assemble all components in `App.tsx`.
- [x] 4.2 Apply final minimalist styling (spreadsheet theme).
- [x] 4.3 Verify all scenarios defined in the specs: adding, editing, deleting, and filtering.
- [x] 4.4 Add a section at the bottom for "Remaining Money".
